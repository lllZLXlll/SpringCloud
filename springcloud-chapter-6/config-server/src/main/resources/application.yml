server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/lllZLXlll/SpringCloudConfig/
          search-paths: /** # git上的配置文件目录，会搜索该目录下的配置文件
          username:
          password:


      label: master
